__pycache__/
*.pyc
*.log
*.err
assets/
transcripts/
day_summaries/
topic_summaries/
votes/
.claude/settings.local.json

# Generated static site — published to the gh-pages branch, never committed on
# the main branch (build with export_static.py / publish with publish_site.sh).
site/

# Per-speaker publish allowlist: names real speakers, so it stays out of the
# template. Copy public_talks.example.txt -> public_talks.txt to create it.
public_talks.txt

# Local-only list of talks awaiting permission (keeps unauthorised names out of git)
public_talks.pending.txt

# Generated slide-cleanup plans (local record of what was applied; reproducible)
/auto_cull.json
/approved_slides.json
/slides_clean.json

# LaTeX build intermediates (from whitepaper.py / ambient_ai_whitepaper.py --pdf)
*.aux
*.fls
*.fdb_latexmk
*.out
*.toc

# Generated white-paper outputs (reproducible from their .py generators).
# Conference summary (root, written locally by alan):
/whitepaper.tex
/whitepaper.pdf
# Ambient AI white paper (whitepaper/ folder, written by Claude):
/whitepaper/ambient_ai_whitepaper.tex
/whitepaper/ambient_ai_whitepaper.pdf
